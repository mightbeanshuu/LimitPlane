Use this loop only for tech-learning tutor maintenance after a session or PDF source exists.

Read `sessions.md`, `progress.md`, the latest session notes, and the latest summary/pattern PDF source if present. Refine the latest summary or pattern PDF source so it is easier to revise from:

- reduce jargon and use simpler English;
- preserve technical accuracy;
- add learner doubts and answers from `sessions.md`;
- add recurring difficult topics and spaced-repetition items;
- add small-code-term explanations for terms like `const`, `await`, `req`, `res`, `findById`, `TTL`, `INCR`, and `EXPIRE`;
- improve visual hierarchy, callouts, tables, glossary, code blocks, and diagrams;
- keep the final result concise and useful without chat history.

If no completed session or PDF source exists, report that there is nothing to refine yet. Do not start implementation work. Do not install dependencies without approval. Log what you did in `progress.md`.
